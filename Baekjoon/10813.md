### 공 바꾸기

```python
N, M = map(int, input().split())
ball = [b for b in range(1, N+1)]
for _ in range(M):
    i, j = map(int, input().split())
    empty = ball[i-1]
    ball[i-1] = ball[j-1]
    ball[j-1] = empty

print(*ball)
```

